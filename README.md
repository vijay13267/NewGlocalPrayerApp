# NewGlocalPrayerApp
Built using react native cli

Steps:

1. npx react-native init NewGlocalPrayerApp --version 0.72.0

2. cd NewGlocalPrayerApp

3.Build using following commands
# For iOS
npx react-native run-ios

# For Android
npx react-native run-android
